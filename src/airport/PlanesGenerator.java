package airport;

import java.util.ArrayList;
import java.util.List;

public class PlanesGenerator {
    public static List<Plane> GeneratePlanes (int date) {
        List<Plane> planes = new ArrayList<Plane>();
        // не сложная генерация по типу и дате отлета
        return  planes;
    }
}
